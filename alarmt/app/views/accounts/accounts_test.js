'use strict';

/*
describe('alarmt.accountsView module', function() {

  beforeEach(module('alarmt.accountsView'));

  describe('accountsView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var accountsCtrl = $controller('AccountsCtrl');
      expect(accountsCtrl).toBeDefined();
    }));

  });
});

 */
