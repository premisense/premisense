'use strict';


/*
describe('alarmt.monitorView module', function() {

  beforeEach(module('alarmt.monitorView'));

  describe('monitorView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var monitorCtrl = $controller('MonitorCtrl');
      expect(monitorCtrl).toBeDefined();
    }));

  });
});

*/