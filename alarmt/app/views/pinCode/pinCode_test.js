'use strict';

/*
describe('alarmt.pinCodeView module', function() {

  beforeEach(module('alarmt.pinCodeView'));

  describe('pinCodeView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var pinCodeCtrl = $controller('PinCodeCtrl');
      expect(pinCodeCtrl).toBeDefined();
    }));

  });
});

*/