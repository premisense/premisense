'use strict';

/*
describe('alarmt.loginView module', function() {

  beforeEach(module('alarmt.loginView'));

  describe('loginView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var loginCtrl = $controller('LoginCtrl');
      expect(loginCtrl).toBeDefined();
    }));

  });
});
*/